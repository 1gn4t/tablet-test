import React from "react";
// import { useUpdateCompaniesMutation } from "../store/companiesApi";

// export const TableCell = ({ type, value, disabled, id }) => {
//   // const [update, result] = useUpdateCompaniesMutation();

//   return (
//     <th>
//       {type === "button" ? (
//         <div style={{ display: "flex" }}>
//           <button>update</button> <button>remove</button>{" "}
//         </div>
//       ) : (
//         <input
//           type={type}
//           value={value}
//           disabled={disabled && true}
//           onChange={(e) => set(e.target.value)}
//         />
//       )}
//     </th>
//   );
// };
